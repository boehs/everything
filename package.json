{
  "versions": {
    "main": "3.0.0",
    "chunks": "0.1.0"
  },
  "scripts": {
    "build": "node ./src/build.js",
    "build:env": "node --env-file=.env ./src/build.js",
    "format": "prettier --write ."
  },
  "dependencies": {
    "all-the-package-names": "^2.0.1230",
    "execa": "^8.0.1",
    "just-split": "^3.2.0"
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  }
}
